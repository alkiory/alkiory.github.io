[TEMPLATE]
# Ejemplo de variables de entorno para alkiory-portfolio
# Copia este archivo a .env y rellena los valores reales (nunca commitear .env)

# Puerto del host mapeado al contenedor (opcional)
PORT=3000
